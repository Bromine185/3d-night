import { Section } from "../Section";

export function MirrorSection() {
  return (
    <Section id="mirror" index="04" title="Mirror" subtitle="The critic reads your journal back to you.">
      <p className="font-mono text-xs text-[var(--night-faint)]">building…</p>
    </Section>
  );
}
