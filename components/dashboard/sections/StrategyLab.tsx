import { Section } from "../Section";

export function StrategyLabSection() {
  return (
    <Section id="lab" index="02" title="StrategyLab" subtitle="One sentence in, a tested strategy object out.">
      <p className="font-mono text-xs text-[var(--night-faint)]">building…</p>
    </Section>
  );
}
