import { Section } from "../Section";

export function StressSection() {
  return (
    <Section id="stress" index="03" title="Stress" subtitle="The breaker's job is to kill it.">
      <p className="font-mono text-xs text-[var(--night-faint)]">building…</p>
    </Section>
  );
}
