import { Section } from "../Section";

export function OvernightSection() {
  return (
    <Section id="overnight" index="01" title="Overnight" subtitle="What the fifty surfaced while you slept.">
      <p className="font-mono text-xs text-[var(--night-faint)]">building…</p>
    </Section>
  );
}
